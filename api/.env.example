# ===== Local Development =====

# Log Analyzer URL (handles log storage, condensing, and forwards to Analysis Service)
# Note: In GCP deployment, this functionality is moved to misc-runner and API routes
LOG_ANALYZER_URL="http://localhost:3001"

# Forge simulation engine path (also used for saved decks in decks/ subdirectory)
FORGE_ENGINE_PATH="../worker/forge-engine"

# Number of parallel Forge containers to run
FORGE_PARALLELISM=4

NODE_ENV="development"

# ===== GCP Configuration =====

# GCP Project ID
GOOGLE_CLOUD_PROJECT="magic-bracket-simulator"

# Cloud Storage bucket for job artifacts
GCS_BUCKET="magic-bracket-simulator-artifacts"

# Pub/Sub topic for job creation events
PUBSUB_TOPIC="job-created"

# Comma-separated CORS origins for /api/* (default: http://localhost:5173)
# In production, include your frontend URL, e.g. https://magic-bracket-simulator.web.app
# CORS_ALLOWED_ORIGINS="http://localhost:5173,https://magic-bracket-simulator.web.app"

# ===== Firebase Auth (optional for development) =====

# For local development, you can use Application Default Credentials:
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/firebase-admin-key.json"

# Or provide the key as a JSON string:
# FIREBASE_ADMIN_KEY='{"type":"service_account",...}'

# Comma-separated list of allowed email addresses (leave empty to allow all authenticated users)
# ALLOWED_EMAILS="user1@gmail.com,user2@gmail.com"

# Worker secret for PATCH/GET jobs from local-worker and misc-runner (optional)
# When set, allows requests with X-Worker-Secret header matching this value
# WORKER_SECRET="your-shared-secret"

# ===== Moxfield API Configuration =====

# Moxfield User-Agent for private API access (required for direct URL import)
# If not set, users must manually paste their deck list for Moxfield decks
# MOXFIELD_USER_AGENT="YourPrivateUserAgent"

# ===== Email Notifications (Access Requests) =====

# Resend API key for sending access request notifications (https://resend.com)
# Free tier: 100 emails/day. If not set, access requests still work but no email is sent.
# RESEND_API_KEY="re_..."

# Admin email to receive access request notifications
# ADMIN_EMAIL="admin@example.com"
