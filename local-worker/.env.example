# GCP Project Configuration
# Download key with: gcloud iam service-accounts keys create ~/magic-bracket-simulator-worker-key.json \
#   --iam-account=magic-bracket-worker@magic-bracket-simulator.iam.gserviceaccount.com
GOOGLE_APPLICATION_CREDENTIALS="/path/to/magic-bracket-simulator-worker-key.json"
GOOGLE_CLOUD_PROJECT="magic-bracket-simulator"

# Pub/Sub Configuration
PUBSUB_SUBSCRIPTION="job-created-worker"

# GCS Configuration
GCS_BUCKET="magic-bracket-simulator-artifacts"

# API Configuration (Cloud Run URL)
API_URL="https://orchestrator-jfmj7qwxca-uc.a.run.app"

# Optional: Auth token for API calls (if required)
# AUTH_TOKEN=""

# Optional: Worker secret for API auth (must match WORKER_SECRET on orchestrator)
# When set, sends X-Worker-Secret header for PATCH/GET jobs
# WORKER_SECRET="your-shared-secret"

# Docker Configuration
FORGE_SIM_IMAGE="forge-sim:latest"
MISC_RUNNER_IMAGE="misc-runner:latest"

# Jobs Directory (where job artifacts are stored locally)
JOBS_DIR="./jobs"

# Polling interval in milliseconds
POLL_INTERVAL_MS=3000
