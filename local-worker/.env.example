# Optional: only needed if you are NOT using gcloud default project or Secret Manager.
# See docs/SECRETS_SETUP.md — goal is no .env on your machine.
#
# No .env needed: run "gcloud config set project magic-bracket-simulator" and
# "gcloud auth application-default login", then "npm run populate-worker-secret"
# once. The worker loads API_URL, GCS_BUCKET, WORKER_SECRET, etc. from Secret Manager.
#
# If you do use a .env (e.g. override or no Secret Manager), you can set:

# GCP (optional — worker also reads from "gcloud config get-value project")
# GOOGLE_CLOUD_PROJECT="magic-bracket-simulator"
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/worker-key.json"

# All below are loaded from Secret Manager when you run populate-worker-secret.
# Only add here to override or if not using Secret Manager:
# PUBSUB_SUBSCRIPTION="job-created-worker"
# GCS_BUCKET="magic-bracket-simulator-artifacts"
# API_URL="https://orchestrator-xxxxx-uc.a.run.app"
# WORKER_SECRET="your-shared-secret"
# FORGE_SIM_IMAGE="forge-sim:latest"
# MISC_RUNNER_IMAGE="misc-runner:latest"
# JOBS_DIR="./jobs"
# POLL_INTERVAL_MS=3000
