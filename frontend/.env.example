# API URL: no .env needed for deployment.
# See docs/SECRETS_SETUP.md. Run "npm run populate-frontend-secret" once, then
# "npm run fetch-frontend-config" before build; the app uses public/config.json at runtime.
# For local dev, config.json is optional — app falls back to localhost.

# Optional overrides for local dev (default: localhost:3000):
# VITE_ORCHESTRATOR_URL="http://localhost:3000"
# VITE_API_URL="http://localhost:3000"

# Firebase Configuration (build-time; needed for auth)
# Get these from Firebase Console > Project Settings > Your apps > Web app
VITE_FIREBASE_API_KEY="your-api-key"
VITE_FIREBASE_AUTH_DOMAIN="magic-bracket-simulator.firebaseapp.com"
VITE_FIREBASE_PROJECT_ID="magic-bracket-simulator"
VITE_FIREBASE_STORAGE_BUCKET="magic-bracket-simulator.appspot.com"
VITE_FIREBASE_MESSAGING_SENDER_ID="your-sender-id"
VITE_FIREBASE_APP_ID="your-app-id"
