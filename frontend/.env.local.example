# Local mode overrides — copy to frontend/.env.local to switch to local mode
# Usage: npm run mode:local (from project root)
#
# .env.local takes precedence over .env — only the variables below are
# overridden; everything else in .env still applies.

# Point to local API server (overrides hosted URL in .env and config.json)
VITE_API_URL=http://localhost:3000

# Disable Firebase Auth → mock local user (empty = no Firebase, no Google sign-in)
VITE_FIREBASE_API_KEY=
