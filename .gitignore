# Plan artifacts - do not commit
agent-plans/

# Environment
.env
**/.env
.env.local
.env.*.local

# Frontend runtime config: config.json is committed with the stable App Hosting URL (not secret — visible when the app loads). Always used as-is.

# GCP credentials and secrets - NEVER commit these
*-key.json
*-credentials.json
firebase-admin-key.json
magic-bracket-worker-key.json
*.pem
service-account*.json

# Node
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__/
*.py[cod]
*$py.class
*.pyc
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
*.egg-info/
.eggs/

# Job store (SQLite DB - shared across processes)
orchestrator-service/data/

# Local worker jobs directory and stable worker ID file
local-worker/jobs/
local-worker/worker-id
orchestrator-service/jobs/

# Forge simulation (generated logs)
forge-simulation-engine/logs/

# Go build artifacts
misc-runner/misc-runner

# Forge installer (too large for GitHub; Dockerfile downloads at build time)
forge-simulation-engine/forge-installer*.jar

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE (optional – remove if you commit shared settings)
.vscode/
.idea/