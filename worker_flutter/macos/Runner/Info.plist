<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconFile</key>
	<string></string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<!-- LSUIElement set to false for MVP. With LSUIElement=true the
	     tray_manager and cloud_firestore native plugins crash on init
	     (uncaught NSExceptions bypass Dart try/catch). Plan 3 fixes this
	     by either signing the app properly or using Swift AppDelegate
	     glue to set up the menu-bar status item without involving
	     tray_manager during cold boot. -->
	<key>LSUIElement</key>
	<false/>
	<key>NSHumanReadableCopyright</key>
	<string>$(PRODUCT_COPYRIGHT)</string>
	<key>NSMainNibFile</key>
	<string>MainMenu</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>

	<!-- Sparkle (auto_updater) configuration.
	     SUFeedURL points at the appcast in this repo on `main`. Updates
	     are surfaced by Sparkle itself; the worker just calls
	     checkForUpdates() at boot + every hour.

	     SUEnableInstallerLauncherService=YES: required by Sparkle 2 when
	     the host app has app-sandbox=NO (which is our case — see
	     Release.entitlements).

	     SUEnableAutomaticChecks=YES: lets Sparkle's scheduled-check
	     timer run.

	     SUPublicEDKey is the public half of the Ed25519 keypair the
	     release workflow uses to sign each update zip. Without this,
	     Sparkle 2 refuses to install updates whose appcast entry lacks
	     `sparkle:edSignature`. The private half lives in the
	     `SPARKLE_ED_PRIVATE_KEY` GitHub Actions secret; the matching
	     local copy is in the macOS Keychain under account `mbs-prod`.

	     ⚠️ Never rotate this without preparing a transition release:
	     existing installs verify against THIS embedded public key, so a
	     change here breaks update install for everyone on the prior
	     version. See worker_flutter/macos/fastlane/SETUP.md. -->
	<key>SUFeedURL</key>
	<string>https://raw.githubusercontent.com/TytaniumDev/MagicBracketSimulator/main/worker_flutter/appcast.xml</string>
	<key>SUPublicEDKey</key>
	<string>+IEgM5RRA9Fq9nlcBaBaZY1jrKAIYu+XT6uvbfAbCfY=</string>
	<key>SUEnableAutomaticChecks</key>
	<true/>
	<key>SUEnableInstallerLauncherService</key>
	<true/>
</dict>
</plist>
